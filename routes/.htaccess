# Bloqueia acesso web direto a esta pasta (código-fonte / config).
# O app acessa estes arquivos via PHP (require), nunca por URL.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
